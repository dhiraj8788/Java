package Interface;

public interface Sample
{
	//Interface
	
	//interface is 100 % OOP's concept(till 1.7 V)
	//Interface is a  blueprint
	
	/*In interface we can declare only incomplete / abstract(till 1.7 V),
	 * public, default, static method(From 1.8 V onwards)
	 * 
	 * We can not create constructor in interface
	 * We can not create object of interface
	 * multiple inheritance is possible in interface
	 */
	
	 int a = 2 ;		//(public static final int a = 2 ;) by default
	
	//Method Declaration
	 
	 void m2();			//(public/default abstract void m2() ) By default 
	 
	 //If we declare methods with defination then there is a compile time error
	

}
